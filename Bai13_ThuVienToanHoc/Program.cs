using System;

namespace Bai13_ThuVienToanHoc
{
    class Program
    {
        static void Main(string[] args)
        {
            // hafm pi
            Console.WriteLine($"Hang so PI{Math.PI}");
            Console.WriteLine("Hello World!");
            // ham max , min
            //Abs - trị tuyệt đối  ,Sign (  return if value = 0 ; return 1 if value > 0 ; return - 1 if value <0) 
            //Luong giac 
            // Console($"sin:{Math.Sin(Math.PI/4)}");
         // tính căn sqrt(a) , pow(a,b) : a mũ b 
         // làm tròn :
         // Math.Round() : làm tròn số nguyên gần nhất 
         // Math.ceiling() làm tròn lên : làm tròn số đó >=a 
         // Math.Floor() làm tròn xuống :
         // Math.Trancate(a): cắt đi phần thập phân lấy 0.4 -> 0 / 0.6 -> 1
        }
    }
}
