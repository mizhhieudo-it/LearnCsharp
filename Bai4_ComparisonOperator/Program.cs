using System;

namespace Bai4_ComparisonOperator
{
    class Program
    {
        static void Main(string[] args)
        {
            // phép toán so sánh // == > < >= <= !=
            // bool isOnline ;
            // isOnline = true ; 
            // isOnline = false; 
            int a = 5 ; 
            int b = 6 ; 
            bool kq = a == b ; 
           // Console.WriteLine(kq);
            // chèn biểu thức vào chuỗi 
            Console.WriteLine($"a != b ==> {a != b}");
            // && cả a và b đều đúng = > true
            // || 1 trong 2 đúng => đúng 
        }
    }
}
